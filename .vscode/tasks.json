{
    "tasks": [
        {
            "type": "shell",
            "label": "Colcon Build",
            "command": "source ~/.shrc && colcon build --packages-select mqsls",
            "args": [
                
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}